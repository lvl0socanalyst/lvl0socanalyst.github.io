$ErrorActionPreference = "Continue"

Write-Host "=== SYSTEM INFORMATION ===" -ForegroundColor Cyan

Write-Host "`n[+] Computer Info"
Get-ComputerInfo | Select CsName, WindowsVersion, WindowsBuildLabEx, OsArchitecture | Format-List

Write-Host "`n[+] Operating System"
Get-CimInstance Win32_OperatingSystem | Select Caption, Version, BuildNumber, OSArchitecture | Format-List

Write-Host "`n[+] Current User"
whoami

Write-Host "`n[+] View all users"
Get-LocalUser

Write-Host "`n[+] Disk Info"
Get-PSDrive -PSProvider FileSystem

Write-Host "`n[+] Domain Info"
cmd /c "net view /domain"

Write-Host "`n[+] Entra Info"
cmd /c "dsregcmd.exe /status"

Write-Host "`n=== END OF RECON ===" -ForegroundColor Cyan